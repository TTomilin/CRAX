# CRAX Requirements
# Python >= 3.10
#
# This file lists the full stack (environments + training + analysis).
# If you only need the environments, use `pip install -e .`. The remaining
# blocks correspond to the [train], [analysis] and [gym] extras in
# pyproject.toml.

# --- Environments and simulation (core install) ---
jax==0.6.0
jaxlib==0.6.0
jax-cuda12-plugin==0.6.0  # For GPU support on CUDA 12.x
jax-cuda12-pjrt==0.6.0

absl-py>=2.3.0
etils>=1.13.0
flax>=0.10.0
jaxopt>=0.8.0
ml_collections>=1.1.0
numpy>=2.2.0
Pillow
typing-extensions

# MuJoCo (physics engine) - IMPORTANT: Keep these versions synchronized!
mujoco==3.11.0
mujoco-mjx==3.11.0
mujoco-warp==3.11.0
warp-lang==1.16.0

# --- Training ([train] extra) ---
optax>=0.2.0
orbax-checkpoint
wandb>=0.19.0
imageio>=2.37.0
matplotlib>=3.10.0

# --- Result download and plotting ([analysis] extra) ---
pandas

# --- Third-party env API wrappers ([gym] extra) ---
gym>=0.26.0
dm_env

# --- Development tools (optional) ---
pytest
ipython
jupyter
